---
// src/components/Logo.astro
import { Image } from 'astro:assets';

const { theme = 'auto' } = Astro.props;

import logoLight from '../../../public/images/logo/logo-light.svg';
import logoDark from '../../../public/images/logo/logo-dark.svg';
---

<!-- Обёртка для логотипа -->
<div class="logo-wrapper" data-theme={theme === 'auto' ? '' : theme}>
  <!-- Логотип для светлой и темной темы -->
  <Image class="logo logo-light" src={logoLight} alt="Логотип (светлая тема)" data-theme="light" />
  <Image class="logo logo-dark" src={logoDark} alt="Логотип (тёмная тема)" data-theme="dark" />
</div>
<!--  -->
<style lang="scss">
  /* Скрываем неактивный логотип */
  .logo {
    display: none;
    border-radius: var(--border-radius-medium);
    width: 100px;
  }

  /* Показываем соответствующий логотип */
  html[data-theme='light'] .logo-light,
  html[data-theme='dark'] .logo-dark {
    display: block;
  }
  /* Для auto - полагаемся на html[data-theme] */
  html[data-theme='light'] .logo-wrapper:not([data-theme]) .light,
  html[data-theme='dark'] .logo-wrapper:not([data-theme]) .dark {
    display: block;
  }
</style>
