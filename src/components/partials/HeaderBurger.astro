---
import Logo from '../ui/Logo.astro';
import Link from '../ui/Link.astro';
import SocialContact from '../ui/SocialContact.astro';
---

<header class="header">
  <div class="header__container">
    <div class="header__logo-wrap">
      <Logo />
    </div>
    <div class="header__menu menu">
      <button type="button" class="menu__icon icon-menu"><span></span></button>
      <nav class="menu__body">
        <ul class="menu__list">
          <li>
            <Link href="/" class="menu__link">Главная<span class="_icon-circle-down"></span></Link>
            <ul class="menu__sub-list">
              <li class="menu__subitem"><Link href="#advantages" class="menu__sublink" data-goto="#advantages">Преимущества</Link></li>
              <li class="menu__subitem"><Link href="#services" class="menu__sublink" data-goto="#services">Услуги</Link></li>
              <li class="menu__subitem"><Link href="#website-types" class="menu__sublink" data-goto="#website-types">Виды сайтов</Link></li>
              <li class="menu__subitem"><Link href="#development-process" class="menu__sublink" data-goto="#development-process">Процесс разработки</Link></li>
              <li class="menu__subitem"><Link href="#result" class="menu__sublink" data-goto="#result">Результат</Link></li>
              <li class="menu__subitem"><Link href="#examples-work" class="menu__sublink" data-goto="#examples-work">Галерея</Link></li>
              <li class="menu__subitem"><Link href="#contact-form" class="menu__sublink" data-goto="#contact-form">Обратная связь</Link></li>
            </ul>
          </li>
          <li><a data-goto=".page__section--2" href="#" class="menu__link">Курсы</a></li>
          <li><a data-goto=".page__section--2" href="/gallery/" class="menu__link">Галерея</a></li>
          <li>
            <button class="menu__link">Новости<span class="_icon-circle-down"></span></button>
            <ul class="menu__sub-list">
              <li class="menu__subitem"><Link href="/news/" class="menu__sublink">Новости IT</Link></li>
              <li class="menu__subitem"><Link href="/post/" class="menu__sublink">Блог</Link></li>
            </ul>
          </li>
          <li><a data-goto=".page__section--3" href="/contacts/" class="menu__link">Контакты</a></li>
        </ul>
      </nav>
    </div>
    <div class="header__social-wrap">
      <SocialContact />
    </div>
  </div>
</header>

<!-- =============== !!-->
<style lang="scss">
  body._lock {
    overflow: hidden;
  }

  /* ========================== */
  .header {
    position: fixed;
    top: 0;
    left: 0;
    width: 100%;
    z-index: 10;
    background-color: var(--md-sys-color-surface-variant);

    &__container {
      display: flex;
      padding-top: 8px;
      padding-bottom: 8px;
      align-items: center;
      justify-content: space-between;
      min-height: 70px;

      @media (min-width: $tablet-small) {
        flex-wrap: wrap;
      }
    }

    &__social-wrap {
      display: none;

      @media (min-width: $tablet-medium) {
        display: block;
      }
    }

    &__logo-wrap {
      width: 180px;
    }
  }

  /* Меню (Mobile First) */
  .menu {
    &__icon {
      display: block;
      z-index: 5;
      position: relative;
      width: 30px;
      height: 18px;
      cursor: pointer;

      span,
      &::before,
      &::after {
        left: 0;
        position: absolute;
        height: 10%;
        width: 100%;
        transition: all 0.3s ease 0s;
        background-color: var(--md-sys-color-on-surface);
      }

      &::before,
      &::after {
        content: '';
      }

      &::before {
        top: 0;
      }

      &::after {
        bottom: 0;
      }

      span {
        top: 50%;
        transform: scale(1) translate(0px, -50%);
      }

      &._active {
        span {
          transform: scale(0) translate(0px, -50%);
        }

        &::before {
          top: 50%;
          transform: rotate(-45deg) translate(0px, -50%);
        }

        &::after {
          bottom: 50%;
          transform: rotate(45deg) translate(0px, 50%);
        }
      }
    }

    &__body {
      position: fixed;
      top: 0;
      left: -100%;
      max-width: 320px;
      width: 100%;
      height: 100%;
      background-color: var(--md-sys-color-surface-container-high);

      padding: 100px 30px 30px 30px;
      transition: left 0.3s ease 0s;
      overflow: auto;
      z-index: -1;

      &._active {
        left: 0;
      }

      &::before {
        content: '';
        position: fixed;
        width: 100%;
        top: 0;
        left: 0;
        height: 70px;
        z-index: 2;
        background-color: var(--md-sys-color-surface-variant);
      }

      @media (min-width: $tablet-medium) {
        max-width: 100%;
        position: static;
        width: auto;
        height: auto;
        background-color: transparent;
        padding: 0;
        overflow: visible;

        &::before {
          display: none;
        }
      }
    }

    &__list {
      li {
        flex-wrap: wrap;
        margin: 0px 0px 30px 0px;

        &:last-child {
          margin-bottom: 0;
        }
      }
    }

    &__link {
      font-size: 24px;
      color: var(--md-sys-color-on-surface);
      display: flex;
      gap: 4px;
      align-items: center;
      width: 100%;
      background: none;
      border: none;
      text-align: left;
      padding: 0;
      cursor: pointer;

      &:hover {
        //text-decoration: underline;
      }
      //1023px и более
      @media (min-width: $tablet-medium) {
        color: inherit;
      }
    }

    &__sub-list {
      position: relative;
      //background-color: var(--md-sys-color-surface-variant);
      flex: 1 1 100%;
      margin: 0;
      padding: 0 12px;
      max-height: 0;
      overflow: hidden;
      transition: all 0.3s ease;
      opacity: 0;
      visibility: hidden;
      will-change: max-height, opacity;

      li {
        //margin: 10px 0;
      }

      @media (min-width: $tablet-medium) {
        position: absolute;
        top: 100%;
        left: 0;
        background-color: var(--md-sys-color-surface-variant);
        //padding: 15px;
        padding: 16px 0px 16px 0px;
        min-width: max-content;
        box-shadow: var(--box-shadow-Light-01);
        transform: translateY(10px);
        pointer-events: none;
        max-height: none;
      }
    }

    &__sublink {
      font-size: 20px;
      //color: tomato;
      display: block;
      //padding: 5px 0;
      padding: 0px 12px 0px 12px;

      &:hover {
        //text-decoration: underline;
      }
    }

    ._icon-circle-down {
      font-size: 14px;
      transition: transform 0.3s ease;
    }

    @media (min-width: $tablet-medium) {
      &__icon {
        display: none;
      }

      &__list {
        display: flex;
        align-items: center;

        > li {
          position: relative;
          margin: 0px 0px 0px 20px;
          padding: 10px 0;
        }
      }

      &__link {
        font-size: 18px;
      }

      &__sub-list {
        li {
          margin: 0px 0px 10px 0px;

          &:last-child {
            margin: 0px;
          }
        }
      }
    }
  }

  /* Анимации для активного состояния */
  .menu__list > li._active {
    .menu__sub-list {
      max-height: 500px;
      opacity: 1;
      visibility: visible;
      margin-top: 20px;
      padding: 12px;
      pointer-events: auto;

      @media (min-width: $tablet-medium) {
        transform: translateY(0);
        margin-top: 0;
      }
    }

    ._icon-circle-down {
      transform: rotate(-180deg);
    }
  }

  /* Поведение для PC и Touch устройств */
  html.no-touch .menu__list > li:hover .menu__sub-list {
    opacity: 1;
    visibility: visible;
    transform: translateY(0);
    pointer-events: all;
  }

  html.no-touch .menu__sublink {
    &:hover {
      background-color: var(--md-sys-color-surface-dim);
    }
  }

  html.touch {
    @media (min-width: $tablet-medium) {
      .menu__list > li {
        display: flex;
        align-items: center;
      }

      .menu__link {
        flex: 1 1 auto;
      }
    }
  }
</style>

<script>
  // Общие элементы
  const iconMenu = document.querySelector('.menu__icon');
  const menuBody = document.querySelector('.menu__body');

  // 1. Инициализация бургер-меню
  if (iconMenu && menuBody) {
    iconMenu.addEventListener('click', function (e) {
      document.body.classList.toggle('_lock');
      iconMenu.classList.toggle('_active');
      menuBody.classList.toggle('_active');
    });
  }

  // 2. Обработчик для плавной прокрутки
  //const menuLinks = document.querySelectorAll('.menu__link[data-goto]');
  const menuLinks = document.querySelectorAll('.menu__sublink[data-goto]');
  if (menuLinks.length > 0) {
    menuLinks.forEach((menuLink) => {
      menuLink.addEventListener('click', function (e) {
        if (this.dataset.goto && document.querySelector(this.dataset.goto)) {
          const gotoBlock = document.querySelector(this.dataset.goto);
          const gotoBlockValue = gotoBlock.getBoundingClientRect().top + scrollY - document.querySelector('header').offsetHeight;

          // Закрываем меню если открыто (бургер)
          if (iconMenu && iconMenu.classList.contains('_active')) {
            document.body.classList.remove('_lock');
            iconMenu.classList.remove('_active');
            menuBody.classList.remove('_active');
          }

          window.scrollTo({
            top: gotoBlockValue,
            behavior: 'smooth',
          });
          e.preventDefault();
        }
      });
    });
  }

  // 3. Обработчик подменю
  const menuItemsWithSub = document.querySelectorAll('.menu__list > li:has(.menu__sub-list)');
  if (menuItemsWithSub.length > 0) {
    menuItemsWithSub.forEach((item) => {
      const button = item.querySelector('.menu__link');

      if (button) {
        button.addEventListener('click', function (e) {
          // Работаем только для touch-устройств
          if (document.documentElement.classList.contains('touch')) {
            e.preventDefault();
            e.stopPropagation();

            if (item.classList.contains('_active')) {
              item.classList.remove('_active');
            } else {
              // Закрываем другие открытые подменю
              document.querySelectorAll('.menu__list > li._active').forEach((activeItem) => {
                if (activeItem !== item) activeItem.classList.remove('_active');
              });
              item.classList.add('_active');
            }
          }
        });
      }
    });

    // Закрытие подменю при клике вне области
    document.addEventListener('click', (e) => {
      if (!e.target.closest('.menu__list > li')) {
        document.querySelectorAll('.menu__list > li._active').forEach((item) => {
          item.classList.remove('_active');
        });
      }
    });
  }
</script>
