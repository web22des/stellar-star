<div class="menu">
  <!-- компонент кнопки бургер в другом места -->
  <nav class="menu__body" aria-label="Основное меню">
    <ul class="menu__list">
      <li class="menu__item"><a href="#" class="menu__link">Главная</a></li>
      <li class="menu__item">
        <button class="menu__link menu__btn-sublink" aria-expanded="false" aria-controls="submenu-1">Подменю</button>
        <ul class="menu__sublist" aria-hidden="true" id="submenu-1">
          <li class="menu__subitem"><a href="#" class="menu__sublink">Пункт 1</a></li>
          <li class="menu__subitem"><a href="#" class="menu__sublink">Пункт 2 очень длинный</a></li>
          <li class="menu__subitem"><a href="#" class="menu__sublink">Пункт 3</a></li>
        </ul>
      </li>
      <li class="menu__item"><a href="#" class="menu__link">Контакты</a></li>
    </ul>
  </nav>
</div>

<style lang="scss">
  .menu {
    padding-top: var(--space-section);
    padding-left: var(--space-all-card);
    height: 100vh;
    max-width: 320px;
    //background: var(--md-sys-color-surface-bright);
    //767px и более
    @media (min-width: $tablet-small) {
      padding-top: 0;
      padding-left: 0;
      height: auto;
      max-width: 100%;
    }
    &__body {
    }

    &__list {
      background: inherit;
      //border: 2px solid #a30909;
      //767px и более
      @media (min-width: $tablet-small) {
        display: flex;
        gap: 52px;
        justify-content: center;
        align-items: center;
      }
    }

    &__item {
      position: relative;
      margin-bottom: 24px;
      //767px и более
      @media (min-width: $tablet-small) {
        position: relative;
        margin-bottom: 0;
      }
    }

    &__link {
      //position: relative;
    }

    &__btn-sublink {
      display: inline-flex;
      align-items: center;
      &::after {
        font-family: 'icomoon';
        font-size: 12px;
        content: '\ea43';
        margin-left: 8px;
        color: inherit;
        display: inline-block; // Необходимо для transform
        transform: rotate(180deg);
        transition: transform 0.5s ease; // Плавная анимация
      }
      &[aria-expanded='true']::after {
        transform: rotate(0); // Поворачиваем при открытом состоянии
      }
    }

    &__sublist {
      // Начальное состояние (скрыто)

      position: absolute;
      top: 24px; // Выпадает прямо под кнопкой
      //left: 12px;
      margin-left: 0;

      padding: var(--space-all-card);
      width: max-content;
      box-shadow: var(--box-shadow-Light-02);
      background: inherit;
      opacity: 0;
      visibility: hidden;
      transform: translateY(-20px); // Начинаем чуть выше
      margin-left: 20px;
      transition: all var(--transition-duration-slow);
      pointer-events: none; // Блокируем клики в скрытом состоянии
      background: var(--md-sys-color-surface-bright);
      // Открытое состояние
      &[aria-hidden='false'] {
        opacity: 1;

        visibility: visible;
        transform: translateY(0);
        pointer-events: auto; // Разрешаем клики
        scroll-behavior: smooth;

        z-index: 10;
      }

      //767px и более Для десктопов
      @media (min-width: $tablet-small) {
        //position: absolute;

        //min-width: 200px; // Чтобы не сжималось
      }
    }

    &__link,
    &__sublink {
      font-size: 18px;
      text-transform: uppercase;
      //767px и более
      @media (min-width: $tablet-small) {
        text-transform: capitalize;
      }
    }
  }
</style>

<script>
  document.addEventListener('DOMContentLoaded', () => {
    const menuButtons = document.querySelectorAll('.menu__btn-sublink');

    // Функция для закрытия всех меню
    function closeAllMenus() {
      menuButtons.forEach((button) => {
        const submenu = document.getElementById(button.getAttribute('aria-controls'));
        button.setAttribute('aria-expanded', 'false');
        submenu.setAttribute('aria-hidden', 'true');
        submenu.classList.remove('menu__sublist-open');
      });
    }

    // Инициализация - закрываем все меню
    closeAllMenus();

    // Обработчик клика по кнопкам меню
    menuButtons.forEach((button) => {
      button.addEventListener('click', (e) => {
        e.stopPropagation();
        const submenu = document.getElementById(button.getAttribute('aria-controls'));
        const isExpanded = button.getAttribute('aria-expanded') === 'true';

        // Закрываем все меню перед открытием текущего
        if (!isExpanded) closeAllMenus();

        // Переключаем текущее меню
        button.setAttribute('aria-expanded', String(!isExpanded));
        submenu.setAttribute('aria-hidden', String(isExpanded));
        //submenu.classList.toggle('menu__sublist-open', !isExpanded);
      });
    });

    // Закрытие при клике вне меню
    document.addEventListener('click', (e) => {
      if (!e.target.closest('.menu__btn-sublink') && !e.target.closest('.menu__sublist')) {
        closeAllMenus();
      }
    });

    // Закрытие при нажатии Escape
    document.addEventListener('keydown', (e) => {
      if (e.key === 'Escape') closeAllMenus();
    });
  });
</script>
